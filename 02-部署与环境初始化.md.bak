# 第二章：部署与环境初始化

> 本章详细介绍 OpenClaw 的多种部署方式与环境初始化配置，帮助用户完成生产级或开发环境搭建。

---

## 2.1 部署模式概览

OpenClaw 支持多种部署模式：

| 模式 | 说明 | 适用场景 |
|------|------|--------|
| 本地单机部署 | Gateway + Agent 运行在同一主机 | 个人开发、单用户 |
| 多 Agent 部署 | 单 Gateway 下配置多个 Agent | 多项目隔离、多角色协作 |
| Docker 容器部署 | 容器化运行，便于迁移和扩展 | 云服务器、CI/CD 环境 |
| 分布式部署 | 多 Gateway + 多 Agent 集群 | 高可用、多团队协作 |

---

## 2.2 Gateway 服务管理

Gateway 是 OpenClaw 的核心网关服务，负责消息路由、Agent 调度和权限管理。

### 安装 Gateway 服务

```bash
openclaw daemon install --port 18789 --token <your-token>
```

### 服务管理命令

```bash
openclaw daemon status   # 查看 Gateway 服务状态
openclaw daemon start    # 启动 Gateway 服务
openclaw daemon stop     # 停止 Gateway 服务
openclaw daemon restart  # 重启 Gateway 服务
openclaw daemon uninstall # 卸载 Gateway 服务
```

### 直接启动 Gateway（调试用）

```bash
openclaw gateway start --host 0.0.0.0 --port 18789
openclaw gateway status
```

---

## 2.3 openclaw.json 核心配置

配置文件位于 `~/.openclaw/openclaw.json`，核心配置项包括：

```json
{
  "gateway": {
    "port": 18789,
    "mode": "local",
    "bind": "loopback",
    "auth": {
      "mode": "token",
      "token": "<your-gateway-token>"
    }
  },
  "channels": {
    "feishu": {
      "appId": "<feishu-app-id>",
      "appSecret": "<feishu-app-secret>",
      "enabled": true,
      "connectionMode": "websocket"
    }
  },
  "skills": {
    "entries": {
      "tavily": {
        "enabled": true,
        "apiKey": "<tavily-api-key>"
      }
    }
  },
  "plugins": {
    "entries": {
      "feishu": { "enabled": true }
    }
  }
}
```

### 常用配置说明

| 配置项 | 说明 |
|--------|------|
| `gateway.port` | Gateway 监听端口，默认 18789 |
| `gateway.bind` | 绑定地址，loopback 仅本地访问 |
| `gateway.auth.token` | Gateway 访问令牌 |
| `channels.*` | 消息渠道配置（飞书、Slack 等） |
| `skills.entries.*` | 技能配置与 API Key |
| `plugins.entries.*` | 插件启用配置 |

---

## 2.4 systemd 服务配置（Linux）

OpenClaw 在 Linux 上使用 systemd 管理 Gateway 服务。

### 服务文件位置

```
~/.config/systemd/user/openclaw-gateway.service
```

### 手动管理服务

```bash
systemctl --user status openclaw-gateway
systemctl --user start openclaw-gateway
systemctl --user stop openclaw-gateway
systemctl --user restart openclaw-gateway
systemctl --user enable openclaw-gateway  # 开机自启
```

### 查看服务日志

```bash
journalctl --user -u openclaw-gateway -f
```

---

## 2.5 Docker 部署

### 使用官方镜像

```bash
docker pull openclaw/openclaw:latest
docker run -d \
  --name openclaw \
  -p 18789:18789 \
  -v ~/.openclaw:/root/.openclaw \
  openclaw/openclaw:latest
```

### docker-compose 示例

```yaml
version: '3.8'
services:
  openclaw:
    image: openclaw/openclaw:latest
    container_name: openclaw
    ports:
      - "18789:18789"
    volumes:
      - ~/.openclaw:/root/.openclaw
    restart: unless-stopped
```

启动：

```bash
docker-compose up -d
```

---

## 2.6 初始化向导

首次安装后推荐运行初始化向导：

```bash
openclaw onboard --install-daemon
```

向导流程：
1. 检测系统环境与依赖
2. 配置 Gateway 端口与认证
3. 设置消息渠道（飞书/Slack/Discord）
4. 安装推荐技能
5. 健康检查与验证

### 跳过特定步骤

```bash
openclaw onboard --skip-channels --skip-skills
```

---

## 2.7 环境变量配置

除 openclaw.json 外，也支持环境变量配置：

```bash
export OPENCLAW_GATEWAY_PORT=18789
export OPENCLAW_GATEWAY_TOKEN=<your-token>
export OPENAI_API_KEY=<your-openai-key>
export ANTHROPIC_API_KEY=<your-claude-key>
```

可写入 `~/.bashrc` 或 `~/.zshrc` 持久化。

---

## 2.8 健康检查与诊断

### 系统诊断

```bash
openclaw doctor        # 全面检测依赖、配置、服务状态
openclaw doctor --fix  # 自动修复可修复的问题
```

### 服务状态

```bash
openclaw status        # 查看主服务状态
openclaw gateway status --deep  # 深度检测 Gateway
```

### 日志查看

```bash
openclaw logs --follow  # 实时查看 Gateway 日志
```

---

## 2.9 常见部署问题

| 问题 | 解决方法 |
|------|--------|
| 端口被占用 | 修改 `gateway.port` 或释放占用端口 |
| 服务无法启动 | 运行 `openclaw doctor` 检测原因 |
| Token 不匹配 | 确认 openclaw.json 和服务配置一致 |
| 无法访问 Gateway | 检查防火墙和 `gateway.bind` 配置 |
| systemd 服务异常 | 运行 `openclaw daemon install --force` 重装 |

---

## 本章小结

- OpenClaw 支持本地、Docker、分布式多种部署模式。
- Gateway 是核心服务，通过 `openclaw daemon` 命令管理。
- 主配置文件 `~/.openclaw/openclaw.json` 控制端口、认证、渠道、技能等。
- 首次安装推荐使用 `openclaw onboard` 完成初始化。
- `openclaw doctor` 提供全面的系统诊断与自动修复能力。

---

> 下一章：Skills 插件体系与批量开发
